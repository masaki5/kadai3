<div class='body'>
 <div class='container'>
  <p id="notice"></p>

    <div class='row'>

      <div class='col-xs-3'>
      <h2>User info</h2>
      <table class='table'>
      <tr><%= attachment_image_tag @user, :profile_image, :fill, 100, 100 %></tr>
      <tr>
        <th>name</th>
        <th><%= @user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= @user.introduction %></th>
      </tr>
      </table>

            <div class='row'>
            <%= link_to "", edit_user_path(@user.id), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>
            </div>
          <div >
          <h2>New book</h2>
          <%= form_for(@books) do |f| %>

          <h4>Title</h4>
          <%= f.text_field :title, class: "col-xs-12" %>

          <h4>Opinion</h4>
          <%= f.text_area :body, class: "col-xs-12" %>

          <%= f.submit "Create Book", class: "btn btn-primary col-xs-12"  %>
          <% end %>
          </div>
      </div>

<div class='col-xs-9'>
  <thead>
      <th><h2>Book detail</h2></th>
       <table class="table">
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
<tr>
<td><%= link_to user_path(@user) do %>
  <%=attachment_image_tag @book.user, :profile_image, :fill, 100, 100 %>
<br><%= @book.user.name %></br>
<% end %>
</td>
<td><%= link_to @book.title, book_path %></td>
<td><%= @book.body %></td>
<td><% if @book.user == current_user %>
  <%=link_to "Edit", edit_book_path, class: "btn-sm btn-success" %>
	<%=link_to "Destroy", book_path(@book.id), method: :delete, class: "btn-sm btn-danger", data:{ confirm: "削除しますか？"} %>
<% end %>
</td>
<th colspan="3"></th>
</tr>
</tbody>
</table>
</thead>
</div>
</div>
</div>
</div>