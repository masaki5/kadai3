<div class='body'>
 <div class='container'>
  <p id="notice"></p>

    <div class='row'>

<%= render 'tenp', user: @user, book: @book %>

<div class='col-xs-9'>
    <h2>Users</h2>

    <table class='table table-hover table-inverse'>
  <thead>
    <tr> 
      <th>image</th>
      <th>nmae</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>
<div>
  <tbody>
  <tr>
  <% @users.each do |user| %>
  <td><%= attachment_image_tag user, :profile_image, :fill, 45, 45 %></td>
  <td><%= user.name %></td>
  <!--<td><%= user.id %></td>-->
  <td><%= link_to "Show", user_path(user.id) %></td>
</tr>
  <% end %>
</tbody>
</div>
</table>
</div>
</div>
</div>
</div>

